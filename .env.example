# .env.example - Environment variables for Nautilus Automação

# Flask Secret Key (generate a strong random key for production)
FLASK_SECRET_KEY='your_flask_secret_key_here_make_it_long_and_random'

# AES Encryption Key for API secrets (MUST be 32 bytes for AES-256)
# Generate a strong random key. Example: openssl rand -hex 32
AES_ENCRYPTION_KEY='your_strong_32_byte_aes_key_here'

# Database URL (Example for PostgreSQL)
# DATABASE_URL='postgresql://user:password@host:port/database'

# Database URL (Example for SQLite - relative to project root)
# Make sure the path is correct based on your app.py configuration
DATABASE_URL='sqlite:///site.db' # This will create site.db in the project root

# Bitget API Passphrase (if required by Bitget for your API key type)
# BITGET_API_PASSPHRASE='your_bitget_api_passphrase'